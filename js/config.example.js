// Copy to js/config.js and fill in from Supabase → Settings → API.
// The anon key is PUBLIC and safe to commit; security is enforced by RLS.
window.PGL_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-KEY"
};
